# Core OCR engines
pytesseract>=0.3.10
easyocr>=1.7.1

# Handwriting transformers (both large models for accuracy)
transformers>=4.40.0
torch>=2.0.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Consensus voting
python-Levenshtein>=0.25.0

# Optional: LLM cleanup pass (set USE_LLM_CLEANUP=True in main.py)
# sentencepiece>=0.1.99
